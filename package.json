{
  "name": "dream-mantra-crm",
  "private": true,
  "version": "1.0.0",
  "description": "Dream Mantra Partner CRM — run backend + frontend together",
  "scripts": {
    "dev": "concurrently -k -n backend,client -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix client\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:client": "npm run dev --prefix client",
    "build": "npm install --prefix backend && npm install --prefix client --include=dev && npm run build --prefix client",
    "start": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
